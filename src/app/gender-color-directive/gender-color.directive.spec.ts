import { GenderColorDirective } from './gender-color.directive';

describe('GenderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new GenderColorDirective();
    expect(directive).toBeTruthy();
  });
});
